window.addEventListener("DOMContentLoaded", function(){
    let users = 
    [
        ["Mari", "Doucet"],
        ["Hugues", "Froger"],
        ["Alex", "Terieur"],
        ["Anne", "O'Neam"]
    ];
    let body = document.querySelector('body');
    
    for(let = i ; i < users.length ; i++){
        
        let tr = document.createElement("tr");
        let prenom = document.createElement("tr");
        
        let users = document.createTextNode(users[i]);
        
        body.appendChild(tr);
        td.appendChild(tr);
        
    }
   
});
